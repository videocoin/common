package resources

// NameSeparator separates the collection IDs and resource IDs that form the
// resource name, providing hierarchical organization.
const NameSeparator = "/"

// Wildcard can be used for resource IDs if they are not required.
const Wildcard = "-"
